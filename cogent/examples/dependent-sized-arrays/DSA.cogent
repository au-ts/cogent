-- CMCL: Subject to change.
type A = U8

type DirEntry = Buffer 2000 ({ >len: A, name_len: U8, name: DArray name_len U8 })

type Entry = { >len : U64
             , name_len : U8
             , name : DArray name_len U8 }

type DirEntry2 = Buffer 2000 Entry

--type DirEntry3 = Buffer 2000 #A