main : () -> R U32 ()
main _ = add (1, 2)

type R a b = < Success a | Error b >

add : (U32, U32) -> R U32 ()
add (x, y) =
    let sum = x + y
    in sum < x || sum < y
        | True -> Error ()
        | False -> Success sum
